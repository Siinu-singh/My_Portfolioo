'use server';
// Flows will be imported for their side effects in this file.

import './ai-instance'; // Add this line to ensure Genkit initializes

// Removed import {defineFirestoreStateStore} from '@genkit-ai/firebase/state-store';
// Removed import {defineFirestoreTraceStore} from '@genkit-ai/firebase/trace-store';
// Removed stateStore and traceStore definitions and export
